/**
 * AT Health Eligibility Checker — Script
 *
 * Populated in Phase 4. Will be wrapped in an IIFE with user-facing
 * handler functions explicitly exposed on window so the inline onclick
 * attributes in the shortcode markup continue to work.
 */
